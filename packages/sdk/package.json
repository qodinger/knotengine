{
  "name": "@tyepay/sdk",
  "version": "0.1.0",
  "description": "Standard Node.js SDK for TyePay — The Non-Custodial Payment Gateway.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@tyepay/types": "workspace:*",
    "@types/node": "^25.2.3",
    "tsup": "^8.5.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
