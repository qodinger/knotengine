import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Disable Tailwind canonical class suggestions if you prefer arbitrary values
      "@next/next/no-arbitrary-classes": "off",
      // Disable image optimization warnings (optional performance suggestions)
      "@next/next/no-img-element": "off",
      // Disable React Compiler incompatibility warnings for React Hook Form
      "react-hooks/incompatible-library": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
